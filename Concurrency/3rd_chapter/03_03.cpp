#include <iostream>
#include <thread>
#include <string>
#include <mutex>
#include <stack>




int main(int argc, const char** argv) {
    
    std::stack<int> s;

    
    // s.push(5);
    // int& value = s.top();
    // std::cout << s.top() << std::endl;
    // value = 8;
    // std::cout << s.top() << std::endl;
    // if (!s.empty())
    // {
    //     int const value = s.top();
    //     s.pop();
    // }

    return 0;
}